# Backend environment example
PORT=4000
FRONTEND_ORIGIN=http://localhost:5173
JWT_SECRET=change-me-in-prod
# Serve the built frontend from backend (set to true in production single-port deploy)
SERVE_STATIC=false